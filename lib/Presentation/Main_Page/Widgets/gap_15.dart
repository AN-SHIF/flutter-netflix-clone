import 'package:flutter/material.dart';

class Gap15 extends StatelessWidget {
  const Gap15({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 15,
    );
  }
}
